<header class="header">
   <h1><%= link_to "Main Page", root_path %></h1>
   <% if user_signed_in? %>
     <%= link_to 'Log Out', destroy_user_session_path, method: :delete %>
     <p>Account: <%= current_user.email %></p>
   <% else %>
     <%= link_to 'Log In', new_user_session_path %>
   <% end %>
</header>