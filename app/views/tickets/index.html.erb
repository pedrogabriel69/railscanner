<h1>Listing Tickets</h1>

<% current_user.tickets.each do |ticket| %>
  <ul>
    <li><%= "№#{ current_user.tickets.index(ticket) + 1 }" %> |
    <%= link_to "#{ticket.initial_station.title} - #{ticket.final_station.title}", ticket %></li>
  </ul>
<% end %>   
